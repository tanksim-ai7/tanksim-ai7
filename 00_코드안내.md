# 사격통제 코드 — 구조와 근거

> 자동 조준 사격 봇 v10 · 2026-08-18 기준
> 코드 비교용 안내입니다. 각 파일이 무엇을 맡는지와, 왜 그렇게 만들었는지를 적었습니다.

---

## 파일 구성

| 파일 | 역할 |
|---|---|
| `auto_aim_bot_v6.py` | 봇 본체. Flask 엔드포인트 · 기동 계층 · 로깅 · 대시보드. **설정(`CFG`)이 여기 있습니다** |
| `fire_control.py` | 사격 통제 핵심. 탄도 해 · 표적 예측 · 데드밴드 · 사격 판정 |
| `enemy_bot.py` | 적 전차 조종. 행동 7종 (`static` `linear` `strafe` `circle` `evade` `charge` `patrol`) |
| `analyze_shots.py` | 사격 로그 분석기. 명중률 · 오차 분포 · 캘리브레이션 |
| `log_columns.py` | CSV 컬럼 한글 사전 |

**적 전차를 우리가 직접 몹니다.** 시뮬레이터 내장 AI 대신 `enemy_bot.py` 를
포트 5100 에 띄워 `Use Enemy Server` 로 연결합니다. 행동을 재현 가능하게 만들기
위해서이고, 7종 모두 결정론적입니다(무작위 요소 없음).

---

## 전체 흐름

```
/info          → 텔레메트리 갱신 (위치 · 자세 · 적 위치)
/get_action    → 매 틱 호출. 아래를 순서대로 수행하고 명령을 돌려줌

  1. 표적 예측     TargetTracker — 비행시간 뒤 적이 어디 있을지
  2. 탄도 해       solve_elevation(거리, 고저차) → 앙각
  3. 사격 판정     세 게이트를 모두 통과해야 발사
  4. 기동          교전 거리를 유지하도록 주행 · 선회 명령
```

---

## 사격 게이트 — 세 관문

발사하려면 **동시에** 통과해야 합니다. 하나라도 막히면 안 쏩니다.

**① 탄도 해가 존재하는가**

```python
th = solve_elevation(거리, dy)     # dy = 아군높이 − 표적높이
```

해가 없으면(너무 가깝거나 멀면) 사격 불가입니다.
**최소 사거리가 고저차의 함수**라는 점이 중요합니다 — `dy` 가 +3 m 면
최소 사거리가 20.5 m 에서 38.4 m 로 거의 두 배가 됩니다.

**② 예상 명중확률이 임계를 넘는가**

```python
p_hit >= p_hit_min      # 기본 0.90
```

재장전이 6.6 초라 헛방이 비쌉니다. 대신 **인내 로직**이 있어서
18 초 넘게 못 쏘면 임계를 0.28 까지 서서히 낮춥니다.

**③ 포탑이 표적을 겨누고 있는가**

```python
|방위오차| <= yaw_db  and  |앙각오차| <= pitch_db
```

데드밴드는 고정값이 아니라 **"예측이 먹고 남은 여유"** 로 매 틱 계산합니다.

```python
lat_room = max(0.25, half_lat - 1.6 * sig_lat) * db_safety
yaw      = degrees(atan(lat_room / 거리))
pitch    = min(lon_room / drdt, pitch_db_max)
```

`half_lat` 은 표적 반폭, `sig_lat` 은 예측 산포입니다.
**예측이 나쁘면 데드밴드가 저절로 좁아집니다.** 고정 비율을 쓰던 이전 버전은
예측이 흔들려도 똑같이 헐거워서 헛방이 많았습니다.

앙각은 `drdt`(앙각 1도당 사거리 변화 [m/°]) 로 나눕니다. 저각 사격이라
`drdt` 가 6~7 정도 되고, **각도 오차가 그만큼 증폭되어 사거리 오차가 됩니다.**

---

## 동결 설정 (v10)

```
body_rate_max = 15.0     차체가 이보다 빨리 돌면 사격 금지
tof_max       = 0.50     비행시간 상한. 넘으면 접근해서 쏨
lon_gain      = 0.15     종방향 조준 이동
pitch_db_max  = 0.15     앙각 데드밴드 상한
p_hit_min     = 0.90     사격 임계 명중확률
band_near     = 26.0     교전 거리 하한
band_far      = 72.0     교전 거리 상한
```

교전 거리는 고정이 아니라 **표적 횡단 속도에 맞춰 매 틱 자동으로 정합니다.**
가까울수록 비행시간이 짧아 예측 오차가 작고(오차 ∝ t²),
멀수록 필요한 포탑 각속도가 작아 추적 여유가 생깁니다.
포탑이 40 °/s 뿐이라, 빠르게 횡단하는 표적은 너무 가까우면
물리적으로 추적 자체가 불가능합니다.

---

## 성적

동결설정 · 교전밴드(≤72 m) · 표적이 이동하는 사격:

```
128발 118명중 = 92.2%   (95% 신뢰구간 86.2 ~ 95.7)
```

교전 형태별 (평지 기준, 총 1,057발):

| 형태 (아군-적) | 발 | 명중률 |
|---|---:|---:|
| 정지-정지 | 156 | 99.4% |
| 이동-정지 | 405 | 94.1% |
| 정지-이동 | 33 | 93.9% |
| **이동-이동** | 429 | **80.2%** |

**이 성적은 전부 평지(Simple Flat)에서 나온 값입니다.**
적이 쏘지 않는 조건에서 측정했습니다(실험 통제).

---

## 측정하면서 알아낸 것

**① 명중률 하락의 절반은 상호작용입니다.**
아군만 기동하면 −5.3 %p, 적만 기동하면 −5.5 %p 인데 둘 다 움직이면
−19.2 %p 입니다. 초과분 **−8.4 %p** 가 상호작용입니다.
상대 방위 각속도가 합쳐져 **조준 오차와 예측 오차가 동시에** 나빠지기 때문입니다.

**② 표적 난이도는 속도나 진폭이 아니라 매끄러움이 정합니다.**
`strafe`(±70° 왕복)가 `evade`(±45° 사행)보다 진폭이 큰데 성적은 8 %p 높습니다.
`strafe` 의 예측 산포가 0.06 m 로 가장 작았습니다 — 주기 14 초 사인파라
등속 예측이 거의 정확히 맞습니다.
반대로 `linear`(직선)는 맵 경계에서 진행 방향을 **180° 순간 반전**해
예측기를 무너뜨립니다.

**③ `tof_max = 0.50` 이 교전 거리를 절반으로 묶고 있었습니다.**
`circle`(적이 아군 주위를 선회) 에서 두 세션 연속 0발이었는데,
표적이 어려워서가 아니라 이 게이트가 막고 있었습니다.
끄니까 57발 55명중 96.5% 가 나왔고 비행시간 중앙값은 0.69 초였습니다.
0.50 초는 이동표적 교전을 약 33 m 안쪽으로 제한합니다.

**④ 지형에서는 '못 맞히는' 것이 아니라 '못 쏩니다'.**
Forest and River 맵에서 쏜 탄은 전부 맞았습니다. 문제는 사격 횟수입니다.
같은 적 행동 · 같은 설정 · 같은 16분으로 비교하면
평지 20발 20명중, 지형 **0발** 입니다.

원인은 조준 정렬입니다. 지형에서 차체가 중앙 5~6°, 최대 26° 기울어지는데
(평지는 정확히 0°) 봇이 `playerBodyY` · `playerBodyZ` 를 읽지 않아
포탑을 방위로 돌릴 때 포신의 월드 앙각이 쓸립니다.
그리고 조준이 맞는 순간의 **99%가 차체 회전 중**이라 `body_rate_max` 게이트에
걸립니다 — 조향에 중간 세기가 없어 오차 4° 만 넘으면 곧바로 최대 40 °/s 로 돕니다.

**⑤ 장애물 차폐는 근거가 없습니다.**
나무가 사격선을 막는 것이 가장 큰 위험이라 예상했으나,
측정한 여덟 조건 전부에서 **장애물에 맞은 탄이 0건**이었습니다.
강 건너 교전 관찰에서도 표적이 사거리 안에 있던 시간이 75%,
추적 가능했던 시간이 93.5% 였습니다. 사거리는 닿았고 표적도 보였습니다.

---

## 비교하실 때 볼 만한 지점

1. **사격 게이트를 몇 개로 나눴는지** — 저희는 셋(사거리 · 확률 · 정렬)으로 분리하고
   매 틱 어느 게이트에 걸렸는지 로그에 남깁니다. 안 쏘는 원인을 사후에 셀 수 있습니다
2. **데드밴드를 고정값으로 뒀는지, 예측 품질에 연동했는지**
3. **차체 자세(기울기)를 조준에 반영하는지** — 저희는 아직 안 합니다. 평지에서만
   측정하다 보니 드러나지 않았고, 지형에서 최대 26° 기울어지는 것을 최근에 확인했습니다
4. **인내 로직 유무** — 빠르게 선회하는 표적은 어떤 거리에서도 P(hit) 이 0.5 를
   넘지 않아, 고정 임계만 쓰면 한 발도 못 쏘고 끝납니다

궁금한 점이나 로그 원본이 필요하시면 말씀해 주세요.
